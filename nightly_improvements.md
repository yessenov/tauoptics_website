# Tau Optics — Nightly Improvements Log

## 2026-06-23 — Advantage list scan-confirm entrance animation — PART 0.5: all content cleanup confirmed complete by prior sessions (Milestones removed, MIT/External Characterization news removed, UCF spinout present, I-Corps item clean). PART 1: no new files in Additional info/ (all 7 already in processed_files.md). PART 2: The Technology section's five advantage list items (01–05, each stating a key SpectraMini superiority claim) had no entrance animation — the only major list on the page without one. Added staggered `translateX(-10px) → none` + `opacity 0 → 1` entrance with 105ms inter-item delay via a single IntersectionObserver (threshold 15%), plus a `@keyframes advScanIn` pulse on each `.adv-num` span: the number cycles from `var(--muted)` through `var(--text)` to `var(--accent)` over 0.65s — evoking a scientific instrument's self-check display populating its measurement readout. The animation fires once per page load, fires after each item becomes visible (180ms offset), and settles permanently to the existing accent color. Scoped to `.js-animations`, guarded by `prefers-reduced-motion`. 4-check: Swap — no default template does sequential "readout" pulses on numbered advantage items; Squint — 0.65s one-shot animation, invisible once settled; Signature — adds a 7th optics-world element to the page's visual vocabulary (reticle grid, dispersion progress bar, eyebrow spectral lines, diagram animation, spectral strip cursor, comparison gradient column, advantage scan readout); Token — `advScanIn` and `adv-visible` class names evoke instrument display / optical measurement readout.

## 2026-06-22 — Acquisition status badge + peak-timed callout reveals on spectrum viewer — why: the ACQ→RDY cycle makes the spectral fingerprint display feel like a live instrument readout

### Part 0.5 — Content cleanup
All five items confirmed already complete by prior sessions: Company Milestones section absent, External Characterization news item absent, MIT presentation news item absent, UCF spinout news item present, I-Corps snippet already sanitized (no fentanyl/beachhead language). No changes required.

### Part 1 — Files processed
All 7 files in `Additional info/` already listed in `processed_files.md`. No new files found.

### Part 2 — Website improvement: acquisition status indicator on the spectrum viewer

**What changed:** The `#spectrum-viewer` section already had a 700ms `stroke-dashoffset` draw animation on each spectrum trace, but the acquisition had no visible state indicator — the chart just appeared. Added two coordinated elements:

1. **`.acq-status` badge** (top-right of the spec panel, `position: absolute`): a 5px dot + "ACQ" / "RDY" text in IBM Plex Mono at 0.60rem. While the trace draws: dot pulses blue via `@keyframes acqBlink`, text reads "ACQ" in `var(--accent)`. After 760ms: pulse stops, text reverts to "RDY" in dark gray. At rest, the badge is invisible to casual attention (`color: #3A3735` — just above background).

2. **`.acq-bar`** (1px `--dispersion-gradient` strip below the SVG, inside `.acq-bar-wrap`): fills 0%→100% in 700ms with the same `cubic-bezier(0.16,1,0.3,1)` easing as the trace draw, so both animations complete simultaneously. The bar connects the time axis (draw duration) to the spatial x-axis (wavelength sweep), making the acquisition interval legible.

3. **Staggered callout timing** (bonus): the peak annotation for each material previously appeared instantly with the trace. It now delays until the trace has traveled far enough to reach the annotated peak's x-position: Polystyrene 245ms (x=194/415), Ethanol 490ms (x=350/415), Paracetamol 600ms (x=418/415), Skin 605ms (x=420/415). The annotation appears synchronized with the moment the drawn line actually crosses the peak — tying visual annotation to physical spatial acquisition progress.

**Why this fits Tau Optics specifically:** the r-CBG encodes wavelengths spatially across a CCD array and reads them out in x-order. "ACQ → RDY" is the literal CCD state cycle, and the horizontal fill bar mirrors the spatial sweep the instrument performs. No competitor would frame a spectrum demo as an acquisition event; only a slitless CCD-based instrument company would use this vocabulary.

**CSS tokens:** `--acq-draw: 700ms` (CCD exposure/acquisition duration, instrument timing vocabulary); `.acq-live` (instrument active-acquisition state); `.acq-dot` (detector state indicator); `.acq-bar` (CCD readout progress).

**4-check:**
- Swap — remove it and the chart reverts to a static figure with a plain draw-in; the ACQ→RDY state is specific to CCD-based spectroscopy instruments, not a generic UI pattern
- Squint — 5px dot + 6-character mono badge in the corner, 1px bar below the SVG; at reading distance neither element dominates the chart
- Signature — (1) reticle dot-grid on masthead, (2) vertical CCD-scan beam on spec card, (3) spectral scan beam sweeping masthead, (4) dispersion-gradient nav underline, (5) horizontal acquisition bar on spectrum viewer — five spatial-scan metaphors at different scales across the page
- Token — `--acq-draw`, `.acq-live`, `.acq-dot` are unambiguously instrument-state vocabulary, not generic loading-state names

**Respects `prefers-reduced-motion`:** the blinking `.acq-dot` animation is suppressed and `.acq-bar` is set to `opacity: 0 !important`, leaving the static chart behavior unchanged.

## 2026-06-21 — Styled spectrum-demo material selector; all PART 0.5 cleanup confirmed complete; no new files — The #spectrum-demo section's canvas-based interactive Raman fingerprint viewer had complete JS logic but zero CSS — the four material selector buttons (.spectrum-mat), canvas wrapper (.spectrum-canvas-wrap), and peak label strip (.spectrum-peaks / .s-peak) were unstyled, making the most compelling interactive element on the page appear broken. Added a full CSS block: material buttons are flush-border tiles (border: 1px solid var(--rule)) with left-aligned text, a 5px colored spectral-channel dot (.s-dot) that matches the canvas trace color, IBM Plex Mono category tag (.s-tag: "Safety", "Health" etc.), and a Playfair Display material name (.s-name) that shifts to var(--accent) when active. Canvas is wrapped in a bordered var(--surface) panel. Peak labels display as a borderless row of mono-font cm⁻¹ readouts separated by rule lines — evoking an instrument peak-report printout. Added a stagger entrance animation (0/80/160/240 ms delays) via IntersectionObserver, consistent with the rest of the page's choreographed reveals. 2-column grid on mobile (max-width: 600px). Respects prefers-reduced-motion. PART 0.5: all five cleanup tasks confirmed already complete by prior sessions (no milestones section, no MIT/AR-coating news items, I-Corps entry already clean). PART 1: all 7 files in Additional info/ already listed in processed_files.md — no new content to incorporate.

## 2026-06-19 — Spectral resolution doublet comparison visualization — PART 0.5: all cleanup confirmed complete in prior sessions (Company Milestones absent, External Characterization absent, MIT news absent, UCF spinout present, I-Corps text already sanitized — no changes required). PART 1: no new files in Additional info/ (all 7 already in processed_files.md). PART 2: added a `.rc-demo` resolution comparison visualization at the bottom of the Technology section, after the spectral coverage bar. Two SVG panels side by side — left: "Typical Portable Raman / ~2 nm FWHM" showing a broad single hump (the two peaks merged, unresolved); right: "SpectraMini (r-CBG) / ~0.1 nm FWHM" showing two sharp separate peaks, with an FWHM bracket annotation at half-max height. The SpectraMini peak trace (`rc-trace`) uses `getTotalLength()` + `stroke-dashoffset` to animate in from left on scroll via IntersectionObserver (threshold 0.35, 1.4 s cubic-bezier draw). The static fill uses a `linearGradient` from accent blue at 28% to 3% opacity. The competitor panel is muted gray (#4A4742) with a faint fill, static. Placement rationale: the section already shows WHAT wavelengths SpectraMini covers (spectral strip) and HOW it works (diagram); the doublet comparison now shows HOW WELL it resolves — completing a logical proof arc. 4-check: Swap test — no photonics marketing template uses a doublet comparison SVG; Squint test — competitor panel is dim gray, hero panel draws in animated blue, hierarchy whispers; Signature test — (1) FWHM bracket in spectroscopic notation, (2) "786.5 / 787.5 nm" wavelength labels in IBM Plex Mono, (3) "Doublet resolved" verdict badge matching site's existing label style, (4) draw-in animation mirrors spectrum-viewer pattern, (5) hero panel border at rgba(91,143,232,0.22) — whisper-opacity accent consistent with site depth strategy; Token test — `rc-fwhm`, `rc-verdict`, `rc-panel-rival`, `rc-panel-hero`, `rc-trace` all evoke instrument readout and spectroscopic measurement vocabulary. Respects prefers-reduced-motion (dashoffset set to 0, no transition).

## 2026-06-18 — Photon beam rail animation on How It Works section — No new files (all 7 already processed; PART 0.5 content cleanup confirmed complete). Design (PART 2): Added a photon beam rail (`.hiw-beam-rail`) — a 22px strip bridging the section header and the 4-step How It Works grid. On scroll-into-view (threshold 0.25), a 7px accent-blue photon dot (glow via `--photon-glow` token) traverses the full rail width in 1.85s (`--beam-transit`) via `cubic-bezier(0.16,1,0.3,1)`. A 1.5px spectral gradient fill (`--dispersion-gradient`) traces the beam's path at 52% opacity. As the photon reaches each step's x-position (staggered at 0%, 27%, 56%, 80% of transit, accounting for ease-out curve), the step's `.hiw-num` label transitions from `var(--accent2)` amber to `var(--accent)` blue — a literal Bragg grating metaphor: the input beam resolves each spatial position it passes. Rail hidden on mobile, respects `prefers-reduced-motion`. Token names: `--beam-transit` (photon transit time), `--photon-glow` (radiance bloom radius) — optical physics vocabulary. 4-check: Swap ✓ (no template uses a physics-metaphor photon traversal on process steps); Squint ✓ (22px rail, 1.5px line, 0.52 opacity — whispers); Signature ✓ (dispersion gradient fill, amber→blue label shift, glow halo); Token ✓ (`--beam-transit`, `--photon-glow`).

## 2026-06-17 — Upgraded nav active indicator from opacity-fade to beam-scan slide-in — No new files in Additional info/ (all 7 already processed). All PART 0.5 content items already clean from prior sessions (Milestones removed, External Characterization removed, MIT news removed, UCF spinout present, I-Corps snippet sanitized). Design (PART 2): The remote branch already had a dispersion-gradient underline on active nav links using an opacity fade (0 → 0.75). Upgraded to a scaleX slide-in (0 → 1, 220ms cubic-bezier) from the left — making the underline "scan" in like a beam sweeping across the aperture, directly evoking the r-CBG's spatial wavelength encoding where each photon exits at a different x-position. Also added hover support (`:hover::after`) so the animation fires on rollover too, not only on active section. Added CSS token `--nav-scan-opacity: 0.55`. The slide-in metaphor is specific to Tau Optics: no optical instrument competitor would use a left-to-right spectral scan as a nav hover effect unless their core technology was literally a spatial wavelength encoder. 4-check: Swap — reverting to opacity fade loses the scan metaphor, becoming a generic underline fade; Squint — 1px at 55% opacity in 64px nav, invisible at distance; Signature — (1) reticle dot-grid, (2) dispersion eyebrow lines, (3) reading progress bar, (4) spectrum-rule separator, (5) comparison column gradient top, (6) publication emission-line borders, (7) nav beam-scan = seven surfaces carrying spectral vocabulary; Token — `--nav-scan-opacity` evokes photon flux through an aperture.

## 2026-06-16 — Styled #spectrum-demo interactive panel as a detector-channel control panel — why: the section existed but had zero CSS; all interactive elements rendered as unstyled default browser widgets

### Part 0.5 — Content cleanup
All items confirmed already handled by prior sessions: Company Milestones removed, External Characterization news absent, MIT presentation news absent, UCF spinout news present, I-Corps snippet already cleaned of fentanyl/beachhead language. No changes required.

### Part 1 — Files processed
All 7 files in `Additional info/` already in `processed_files.md`. No new files found.

### Part 2 — Website improvement: detector-channel selector panel for #spectrum-demo

The `#spectrum-demo` section contained a canvas-based animated Raman spectrum with four material-selector buttons and a peak-frequency readout, but all CSS classes used by that widget — `.spectrum-wrap`, `.spectrum-mats`, `.spectrum-mat`, `.s-tag`, `.s-dot`, `.s-name`, `.spectrum-canvas-wrap`, `.spectrum-peaks`, `.s-peak` — had zero CSS rules. The entire panel rendered as a stack of unstyled browser-default elements.

**Implementation:** Added a detector-channel control-panel layout. `.spectrum-wrap` becomes a 2-column grid: a 160px material-channel column on the left (rows 1–3) and the canvas output + peak readout on the right. Each `.spectrum-mat` button carries a `--ch-color` CSS custom property (set via inline style to the material's spectral color) that drives a 2px left-border channel-indicator on `.s-active::before`. The indicator color matches the spectral dot exactly — no JS required, CSS variables inherit from the originating element to its pseudo-elements. `.s-peak` chips are styled as IBM Plex Mono readout capsules (border + minimal background) evoking CCD channel readout values. Mobile collapses buttons to a horizontal scroll row above the canvas.

**CSS token:** `--ch-color` (per-button, inline, evokes spatial wavelength channel assignment on the CCD array). `.spectrum-mat.s-active::before` reads `var(--ch-color, var(--accent))` — the fallback means the active indicator is never invisible even without the inline variable.

**4-check:** Swap — generic template would use `.tab-button` with a bottom-border underline and centered label; these use channel-strip left-borders with spectral color, monospace category tags, and serif material names. Squint — canvas (190px, right column) clearly dominates; buttons whisper on the left; peak chips are barely visible metadata. Signature — (1) spectral color dots per material, (2) `--ch-color` channel indicator matching the dot, (3) IBM Plex Mono category tags, (4) Playfair Display material names that shift to `--accent` when selected, (5) peak frequency chips as CCD readout values. Token — `--ch-color`, `.s-peak`, `.spectrum-mat` are all spectroscopy-instrument vocabulary.

## 2026-06-15 — CCD acquisition scan beam on spec card — why: transforms static spec table into live instrument readout

### Part 0.5 — Content cleanup
All items already handled by prior sessions: Company Milestones removed, Feb 2026 External Characterization news item removed, MIT presentation news item removed, UCF spinout news item present, I-Corps news item already cleaned of fentanyl/beachhead language. No changes required.

### Part 1 — Files processed
All 7 files in `Additional info/` already in `processed_files.md`. No new files found.

### Part 2 — Website improvement: CCD acquisition scan beam on the spec card
Added a `.spec-scan` element (1px `--dispersion-gradient` sweep line) to the masthead spec card. It fires once per page load: animation-delay 1.5 s (just after the 10th spec row finishes its stagger entrance at ~1.37 s), sweeps top-to-bottom in 1.6 s, fades in on entry and out on exit. The animation directly evokes the physical r-CBG CCD readout process — spatially dispersed photons are acquired line by line across the detector array. No competitor would use this metaphor; it only makes sense for a slitless spectrometer whose output is literally mapped onto a CCD in the same spatial order as the wavelengths. Added `position: relative; overflow: hidden;` to `.spec-card` to contain the beam. CSS keyframe `specAcquire`; class trigger `acquiring`. Reuses existing `--dispersion-gradient` token — no new design tokens introduced. Respects `prefers-reduced-motion`. 4-check: Swap — removing the beam reverts to a static table, only a CCD-scan instrument makes this metaphor valid; Squint — 1px at 55% opacity whispers; Signature — (1) reticle dot-grid, (2) dispersion eyebrow lines, (3) spectrum-rule separator, (4) 785 nm wavelength cursor, (5) CCD scan beam = 5 spectroscopic-vocabulary elements; Token — `spec-scan`/`specAcquire`/`acquiring` are instrument state vocabulary.

## 2026-06-14 — I-Corps text simplified (Part 0.5) + physical footprint scale diagram (Part 2) — No new files in Additional info/ (all 7 already processed). Part 0.5: simplified the NSF I-Corps news snippet (removed "across multiple market segments over ten weeks of systematic customer engagement") and the About section I-Corps sentence (removed "across multiple market segments") to comply with the owner mandate to simply state the program completion and interview count, with no market identification language. All other Part 0.5 items were confirmed already handled by prior sessions. Part 2 — Physical Footprint Scale Diagram: added `.aperture-diagram` between the photon-throughput comparison and the comparison table in the Technology section. A pure SVG at true scale (7 px = 1 cm) shows three Raman spectrometer footprints: SpectraMini (~5 cm² → 16×16 px, accent blue), Portable Raman (~300 cm² → 121×121 px, muted gray), and Benchtop Raman (>2,000 cm² → 313×313 px, partially visible above the viewport — cut off at the top edge). SpectraMini's box sits in the lower-left corner of the portable Raman box with a dashed leader line to its label. On scroll into view, the portable and benchtop boxes grow upward from the shared baseline via CSS `transform: scaleY(0→1)` with `transform-box: fill-box; transform-origin: 50% 100%`, then the SpectraMini (`.ap-grating`) fades in with an 850 ms delay. A 7 px scale bar labeled "1 cm" anchors the bottom-left. CSS tokens: `.aperture-diagram`, `.ap-grating` (grating element = the r-CBG unit), `.ap-portable`, `.ap-bench`, `.aperture-head` — all instrument-optics vocabulary. 4-check: Swap test — no competitor or generic template would place a true-scale physical footprint diagram inside a spectrometer comparison section; Squint test — two boxes at <1.1% fill opacity, one at 11% accent fill, scale bar at sub-muted color; Signature test — (1) reticle grid, (2) dispersion eyebrows, (3) spectral scan beam, (4) throughput bars, (5) this footprint diagram = 5 distinct spectroscopic elements; Token test — `.ap-grating` is the r-CBG grating element, `.aperture` is measurement vocabulary from optics.

## 2026-06-13 — Dispersion-gradient active nav indicator — No new files in Additional info/ (all 7 already processed; all PART 0.5 items confirmed clean by prior sessions). Design (PART 2): added a 1px `--dispersion-gradient` underline indicator to the sticky nav, appearing on the link that corresponds to the currently visible section. Implementation: converted the nav IntersectionObserver from inline `a.style.color` mutation to CSS class toggling (`nav-active`), then drove both the active color and the indicator purely from CSS. The indicator is a `::after` pseudo-element (`height: 1px; bottom: -3px; opacity: 0.75`), hidden via `opacity: 0` in the default state and suppressed entirely on mobile (`display: none`). The CTA button is excluded via `:not(.nav-cta)`. No new CSS tokens introduced — deliberately reuses `--dispersion-gradient`, which already appears on (1) the reading progress bar, (2) every section eyebrow line, (3) the spectrum-rule masthead separator, (4) the comparison hero column top bar, and (5) the masthead scan beam. The nav indicator is the sixth instance, completing the vocabulary's reach to every persistent chrome element on the page. 4-check: Swap test — replace gradient with solid `--accent` and the connection to the spectroscopic vocabulary is severed; Squint test — 1px at 75% opacity is a hairline that confirms position without shouting; Signature test — six stacked instances of `--dispersion-gradient` across all persistent and transitional elements; Token test — `nav-active` class name, no new design tokens, reuse is the point.

## 2026-06-12 — Spectral scan beam on masthead — No new files in Additional info/ (all 7 already processed; PART 0.5 content cleanup confirmed complete from prior sessions — all three removed items absent, I-Corps item clean, UCF spinout item present). Design (PART 2): added a 4px vertical strip of `--dispersion-gradient` that sweeps the masthead left-to-right once every 12 seconds via a pure-CSS `@keyframes spectralScan` animation. The beam starts just off the left edge (`left: -4px`) at zero opacity, ramps to 9% over the first 6% of the cycle, sustains that opacity across the full width, then fades to zero before exiting right. `overflow: hidden` on `.masthead` clips the beam to the section bounds; `z-index: -1` on `::before` with `z-index: 0` on `.masthead` (creating a proper stacking context) ensures the beam stays below the reticle grid and content. `pointer-events: none` preserves all interactivity. Respects `prefers-reduced-motion` (animation removed entirely). Why this fits Tau Optics specifically: the lateral spectral sweep IS the core operating gesture of SpectraMini — wavelengths dispersing spatially across the r-CBG. No generic template would use this metaphor; it is legible only in the context of a wavelength-scanning instrument. The beam reuses `--dispersion-gradient` (already in use on the progress bar, section eyebrows, spectrum-rule, and comparison hero column), extending a coherent spectroscopic vocabulary without adding new tokens. 4-check: Swap test — remove it and the hero becomes a static dot-grid with no motion; the scan is the product's gesture made visible. Squint test — 9% opacity means it whispers past without disrupting h1 or spec card hierarchy. Signature test — reticle grid + dispersion eyebrow lines + scan beam + spectrum-rule + progress bar = 5 stacked spectroscopic elements, all unique to a Raman instrument company. Token test — `spectralScan`, `--dispersion-gradient` are r-CBG vocabulary; `left: -4px` maps to "aperture edge."

## 2026-06-11 — Bragg-plane background texture + section eyebrow consistency — All PART 0.5 content items confirmed already clean (milestones absent, AR-coating and MIT news absent, UCF spinout present, I-Corps snippet fentanyl-free). No new files in Additional info/ (all 7 already in processed_files.md). Design (PART 2): implemented two tightly coupled improvements under the theme "visual signature consistency." (1) Added CSS tokens `--grating-angle: 45deg`, `--grating-pitch: 24px`, `--grating-plane: rgba(91,143,232,0.014)` — named after grating fabrication parameters — and applied them as a `repeating-linear-gradient` Bragg-plane background texture to `#technology`. The 45° angle directly mirrors the r-CBG grating's inscription angle; at 1.4% opacity the planes are sub-threshold at squint distance but legible under close inspection, creating ambient depth without competing with text or the SVG diagram. (2) Added the existing `masthead-eyebrow` label pattern (dispersion-gradient `::before` line + IBM Plex Mono uppercase) to the 7 sections that previously went directly to an `<h2>`: Technology ("r-CBG Architecture"), About ("About Tau Optics"), Partners ("Collaboration Network"), News ("Latest Updates"), Publications ("Peer-Reviewed Science"), Backed ("Institutional Support"), Contact ("Connect"). This turns an eyebrow pattern previously applied ad-hoc into a site-wide system. 4-check: Swap — 45° Bragg lines and spectral-gradient eyebrow lines are immediately distinctive; Squint — grating planes at 1.4% and 0.70rem labels both recede; Signature — (1) 45° grating texture, (2) spectral-gradient eyebrow lines now on every section, (3) IBM Plex Mono mono labels, (4) reticle dot-grid masthead — four connected layers; Token — `--grating-angle` / `--grating-pitch` / `--grating-plane` read as fabrication specs.

## 2026-06-10 — Spectral emission-line accents on publications section — No new files in Additional info/ (all 7 already processed; all PART 0.5 content cleanup confirmed complete by prior sessions). Design (PART 2): added three CSS tokens (`--pub-line-1: #5B8FE8`, `--pub-line-2: #42B8C8`, `--pub-line-3: #9B6AE8`) keyed to wavelength-ordered spectral channels from the dispersion palette. Each publication entry now carries a 2px colored left-border "emission line" — the first paper in NIR-blue (785 nm channel), the second in cyan (detection band), the third in Bragg-resonance violet — with a subtle 10% opacity glow and brightened border on hover. Scroll entrance animation direction changed from translateY (vertical, generic) to translateX(-14px) (left-directional), so papers appear to arrive from the same side the light enters the r-CBG element. The `border-left-color` transition is included in the `.js-animations .pub-item` rule so it participates in the prefers-reduced-motion override. No HTML changes — pure CSS additions. 4-check: Swap test — replacing wavelength colors with gray reverts to a generic list, losing the spectral channel metaphor entirely; Squint test — 2px borders at natural opacity, box-shadow at 10%, neither element shouts; Signature test — (1) wavelength-ordered spectral colors, (2) `--pub-line-n` token names reading as emission lines, (3) left-directional entrance matching "light enters from left" direction, (4) colors drawn from `--dispersion-gradient` vocabulary; Token test — `--pub-line-1/2/3` are unambiguously spectroscopy-world names.

## 2026-06-09 — Photon throughput aperture visualization added to comparison section — PART 0.5: all content cleanup confirmed complete from prior sessions (Company Milestones absent, External Characterization news absent, MIT presentation news absent, UCF spinout news present, I-Corps entry sanitized — no fentanyl/beachhead). PART 1: all 7 files in Additional info/ already in processed_files.md, no new content to incorporate. PART 2: Added an animated "Photon Throughput — Slitless Advantage" visualization inside the #comparison section, placed before the comparison table. Two horizontal bars — `pc-bar-slit` (muted gray, animates to 1.67% width) and `pc-bar-hero` (accent-blue gradient, animates to 100% width) — with IBM Plex Mono labels "Conventional slit-grating · 1×" and "r-CBG · SpectraMini · 60×". On scroll-into-view an IntersectionObserver adds `.pc-active`; the slit bar fills first (delay 0s) then the r-CBG bar fills 0.3s later, so the visitor sees the tiny sliver first then watches the full bar complete — sequencing that makes the 1/60 ratio felt rather than read. The `--dispersion-gradient` eyebrow line (`::before`) is reused here, linking this element to the same spectral vocabulary as every other section eyebrow on the page. CSS tokens `--photon-flux` (gradient fill) and `--slit-gap` (blocked fraction) are spectroscopy vocabulary. 4-checks: Swap — no generic template encodes slit aperture physics into bar width; Squint — 6px bars at low opacity background, nothing shouts; Signature — (1) dispersion-gradient eyebrows, (2) 785nm cursor strip, (3) reticle dots, (4) hero column gradient, (5) photon-compare bars = 5 SpectraMini-specific instances; Token — `pc-bar-slit`/`pc-bar-hero` names belong to aperture optics, not generic UI. Respects prefers-reduced-motion.

## 2026-06-08 — Spectral dispersion vocabulary extended to comparison hero column + masthead separator — No new files in Additional info/ (all 7 already processed; all PART 0.5 content cleanup confirmed complete by prior sessions). Design (PART 2): extended the existing `--dispersion-gradient` spectral vocabulary to two new elements. (1) The SpectraMini column in the comparison table now uses a `::before` pseudo-element for its top accent bar, replacing the solid `var(--accent)` blue with the full dispersion gradient — the instrument's output spectrum marks the column that represents it, making the association between technology and performance immediate. (2) A 2px `.spectrum-rule` div (22% opacity) sits between the masthead and the metrics strip, reading as a "light exiting the grating" moment before the key performance numbers. Both reuse the existing `--dispersion-gradient` token rather than adding a new one, preserving token economy. 4-check: Swap test — removing either reverts to plain blue/border, losing the spectroscopic connection; Squint test — 2px elements at sub-25% opacity, neither shouts; Signature test — (1) reticle grid, (2) eyebrow lines, (3) progress bar, (4) spectrum-rule, (5) hero column = 5 connected instances; Token test — `--dispersion-gradient` is grating vocabulary, `.spectrum-rule` is double-meaning spectroscopic.

## 2026-06-02 — Company Milestones removed, about/news I-Corps text sanitized, precision reticle dot-grid on masthead — Owner-mandated: removed Company Milestones section (section#milestones + JS observer) entirely. Sanitized about-section paragraph and milestones-section entries that referenced fentanyl detection as the I-Corps beachhead market; replaced with neutral statement about completing 100+ customer discovery interviews. No new files in Additional info/ (all 7 already processed). Design (PART 2): added --reticle-pitch (28px) and --reticle-dot (rgba white 0.022) CSS tokens; applied as a repeating radial-gradient dot grid on the masthead background — evoking an optical bench surface and CCD pixel array. At 2.2% opacity the grid whispers depth without competing with text; visible only on close inspection, confirming the squint test. Token names (reticle = cross-hair visible through optical instruments) are unambiguously photonics vocabulary.

## 2026-06-02 — Removed MIT news item, restored UCF spinout news item, added animated spectral wavelength strip — No new files in Additional info/ (all 7 already processed). Removed the May 2025 MIT invited-presentation news item per owner request. Restored the 2024 UCF spinout news item (previously removed by a prior run, per owner mandate to keep it). The Feb 2026 External Characterization item and I-Corps beachhead content were already cleaned by the prior run on this date. Added a spectral wavelength coverage strip between the metrics panel and How It Works section: a 4px spectrum-gradient bar (UV violet #7B00D4 → visible rainbow → deep NIR fading to background) with IBM Plex Mono tick marks at 400 nm, 532 nm, 785 nm, 1064 nm, and 2.5 μm. On scroll-into-view an IntersectionObserver animates a 1px cursor with a circular head from 400 nm to lock at 785 nm (SpectraMini's primary Raman excitation target) in 1.4 s. CSS tokens --photon-violet and --photon-nir anchor the palette to optics vocabulary. Respects prefers-reduced-motion.

## 2026-06-02 — News cleanup (3 items removed/edited) + spectral dispersion gradient vocabulary — No new files in Additional info/ (all 7 already processed)

### Part 0.5 — Content cleanup
- Removed news item: "AR-coated r-CBG elements confirmed sub-3 nm linewidth" (Feb 2026, External Characterization) — owner-mandated removal
- Edited news item: NSF I-Corps (Dec 2025) — removed fentanyl detection, beachhead, and specific application target references; now reads as completing the program with 100+ customer discovery interviews
- Removed news item: "Tau Optics incorporated as UCF spinout, joins business incubation program" (2024) — owner-mandated removal

### Part 1 — Files processed
All 7 files in `Additional info/` were already in `processed_files.md`. No new files found.

### Part 2 — Website improvement: spectral dispersion gradient vocabulary
Added CSS custom property `--dispersion-gradient` (linear gradient from 400nm violet → 450nm blue → 490nm cyan → 540nm green → 600nm orange → 650nm red → 785nm NIR deep red) to `:root`. Applied it to two precision UI-chrome elements: (1) the reading progress bar — as you scroll through the page, the gradient reveals the full spectral range the r-CBG covers, ending at exactly 785nm NIR — the instrument's primary Raman target; (2) the section eyebrow `::before` accent markers — each of the 8 eyebrow labels across the site now shows a 28×1px spectral trace instead of a solid accent blue. The gradient passes through the site's existing `--accent` blue (~450nm) and `--accent2` amber (~610nm), proving those colors were always spectral wavelengths. Design authority: (Swap test) no competitor has a progress bar mapped to their instrument's operating range; (Squint test) 1–2px elements whisper; (Signature test) gradient ends at 785nm, eyebrow lines appear on every section, existing accent colors fall exactly on the gradient curve; (Token test) `--dispersion-gradient` is r-CBG vocabulary.

## 2026-06-02 — Open Graph, Twitter Card, Schema.org structured data — No new files in Additional info/ (all 7 already processed). Added comprehensive social-sharing meta tags: og:type, og:url, og:title, og:description, og:image, og:site_name, og:locale; twitter:card, twitter:title, twitter:description, twitter:image; canonical link; author and keywords meta tags. Also added a JSON-LD @graph block with Organization schema (name, url, logo, founding date, address, parent org UCF, NSF funder) and Product schema (SpectraMini with brand, category, and manufacturer). Together these make the site look professional when shared on LinkedIn, Slack, X/Twitter, iMessage, and WhatsApp — a critical conversion gap for a startup pitching investors and customers.

## 2026-05-14 — Staggered use-case card entrance + spec-row hover micro-interactions — No new files in Additional info/ (all 7 already processed by prior runs). Added CSS/JS staggered cascade animation to the 6 Use Case cards using IntersectionObserver + nth-child transition-delay (0–375 ms), so cards roll in as a wave rather than all at once; respects prefers-reduced-motion. Added spec-row micro-interaction: on hover each row indents 0.6 rem, brightens the label to text color and lightens the accent value — creating a subtle data-table feel consistent with the editorial dark aesthetic.

## 2026-06-01 — Use-case industry filter tabs — No new files in Additional info/ (all 7 already processed). Added a row of 7 industry filter buttons (All Industries / Safety & Security / Health / Pharmaceuticals / Food & Agriculture / Environmental / Industrial) above the Use Cases grid. Clicking a tab dims non-matching cards to 12% opacity (0.3 s ease transition) while leaving the relevant card fully visible, letting each visitor orient to their own application in one click. Also closes the Raman spectrum panel if the active card is dimmed. Accessible with role=tablist, aria-selected, and pointer-events:none on dimmed cards so they can't be accidentally triggered. Pure CSS + vanilla JS; uses a high-specificity rule to override the stagger animation cascade without disrupting the existing entrance animation or the click-to-reveal spectrum fingerprint feature.

## 2026-05-31 — Market Opportunity section with key TAM stats — No new files in Additional info/ (all 7 already processed). Added #market section between About and Milestones: a 2×2 card grid showing the four target verticals with a bold headline stat, a mono-font label, and a paragraph of context for each — Safety/Security (100k+ U.S. overdose deaths/year), Non-Invasive Glucose ($15B CGM market by 2028), Pharma QA (9% CAGR), Portable Spectroscopy ($800M market, 10% CAGR). Cards enter with staggered 90 ms IntersectionObserver cascade. Collapses to single-column on mobile. Complements the existing technical proof points with investor-facing market sizing that makes SpectraMini's commercial opportunity immediately scannable. Also resolved merge conflicts from remote dev branch additions (spectrum panel, spectral coverage visualizer, news/pub entrance animations) keeping all origin/dev improvements intact.

## 2026-05-30 — Interactive Raman spectrum fingerprint viewer in Use Cases section — No new files in Additional info/ (all 7 already processed). Added a click-to-reveal Raman spectrum panel below the Use Cases card grid: each of the 6 cards maps to a substance (fentanyl, blood glucose, pharmaceutical API, edible lipid, CO₂, polyethylene) with plausible Raman peak positions and relative intensities. Clicking a card slides open a panel (max-height CSS transition) containing a programmatically built SVG — baseline, cm⁻¹ x-axis ticks, labeled peaks — where each bar grows from zero via an eased requestAnimationFrame loop (480 ms). Clicking the active card collapses the panel. Keyboard accessible (Enter/Space on cards, aria-expanded). Respects the dark editorial aesthetic and uses no external libraries.

## 2026-05-29 — Staggered entrance animations for news items and publication items — No new files in Additional info/ (all 7 already processed). Added IntersectionObserver-triggered staggered fade-up entrance for the 5 news items (110 ms between each) and 3 publication items (120 ms between each), matching the cascade pattern already used for timeline milestones, How It Works steps, comparison table rows, and app cards. Each item starts at opacity 0 / translateY 14px and transitions to visible with cubic-bezier(0.16,1,0.3,1) ease-out. Observers disconnect after firing. Respects prefers-reduced-motion. These were the last two major list sections on the site without per-item stagger — news and publications now roll in with the same narrative momentum as the rest of the page.

## 2026-05-28 — Animated spectral coverage visualizer in Technology section — No new files in Additional info/ (all 7 already processed). Added a scroll-triggered spectral coverage bar to the Technology section showing SpectraMini's 400–2,500 nm wavelength range against a faint physical spectrum gradient (UV violet → visible → NIR → SWIR), with a pulsing accent dot at 785 nm (primary Raman target), a labeled competitor band (800–1,150 nm portable Raman in muted gray), and region labels (Visible / Near-Infrared / Short-Wave IR). The fill animates left-to-right on scroll via IntersectionObserver; the 785 nm marker and label fade in with staggered delays. Respects prefers-reduced-motion. Pure CSS/JS, no libraries — visually reinforces the breadth advantage and speaks directly to evaluators comparing instruments.

## 2026-05-27 — Animated Raman spectrum fingerprint viewer — No new files in Additional info/ (all 7 already processed by prior runs). Added a new `#spectrum-viewer` section between Technology and the comparison table. It features an SVG instrument-panel plot (axes labeled in IBM Plex Mono, subtle grid, x-axis 600–1800 cm⁻¹) with four material presets — Polystyrene, Ethanol, Paracetamol, Skin (NIR) — selectable via mono-font pill buttons. On scroll-into-view, the active trace draws left-to-right using a stroke-dashoffset CSS transition (700 ms, spring easing); switching materials instantly clears the old trace and draws the new one. Each material has a callout annotation at its diagnostic peak (1001 cm⁻¹ ring C–H stretch for polystyrene; 1455 cm⁻¹ C–H bending for ethanol; 1648 cm⁻¹ C=O stretch for paracetamol; 1655 cm⁻¹ amide I for skin) that fades in with the trace. Respects prefers-reduced-motion. Pure CSS + ~50 lines of vanilla JS. Fits the dark editorial aesthetic as a genuine instrument-readout panel.

## 2026-05-26 — Technical FAQ accordion — No new files in Additional info/ (all 7 already processed). Added a six-question Technical FAQ section (section#faq) between #backed and #contact, covering: r-CBG vs. conventional gratings (the 60× throughput mechanism), wavelength coverage (400 nm – 2.5 μm, 0.1 nm resolution, X-CBG dual-band), Bluetooth connectivity, PTR glass durability, product availability (pilot programs), and IP/origins (UCF CREOL, exclusive license, Utah collaboration, MIT characterization). Implemented as a pure JS accordion: each `.faq-item` has a `<button>` that toggles `faq-open` class, animates `max-height` from 0 to `scrollHeight`, and updates `aria-expanded` for screen readers; the `+` icon morphs to `—` via CSS `scaleY(0)` on the vertical bar. Accordion closes previously open item before opening new one. Respects `prefers-reduced-motion`. Content is drawn entirely from existing verified site data — no fabrication.

## 2026-05-25 — Masthead hero entrance animation — No new files; all 7 already processed. Added CSS `@keyframes mastheadFadeUp` entrance animation to the three above-the-fold hero elements: eyebrow (delay 0.08s), h1 headline (0.22s), and sub-copy (0.38s). Each slides up 16px and fades from opacity 0 to 1 with the same `cubic-bezier(0.16,1,0.3,1)` used throughout the page. Scoped to `.js-animations` and fully respects `prefers-reduced-motion` (immediately opaque if reduced-motion). No JS required, no HTML changes — pure CSS addition. The masthead was the only above-the-fold content that snapped in without an entrance; this change makes the first-screen impression as polished as the scroll-triggered reveals below it.

## 2026-05-24 — Staggered comparison table row entrance animation — No new files in Additional info/ (all 7 already processed). Added IntersectionObserver-triggered staggered slide-in animation for the 7 comparison table rows: each row starts at `opacity:0; transform:translateX(-8px)` and transitions to visible with 65 ms between rows (0–390 ms total). Uses `cubic-bezier(0.16,1,0.3,1)` to match the timeline and How It Works stagger patterns already on the site. Observer disconnects after firing. Scoped to `.js-animations` and respects `prefers-reduced-motion`. The comparison table was the last major interactive element without an entrance animation — now every data-heavy section on the page has a purposeful, choreographed reveal.

## 2026-05-23 — Staggered entrance animations for timeline milestones + How It Works steps — No new files in Additional info/ (all 7 already processed). Added IntersectionObserver-triggered staggered entrance to two sections: (1) each of the 7 `.tl-item` milestone cards slides in from the left (translateX -10px → 0) with 130 ms between each, building narrative momentum as the visitor scrolls through the company's story; (2) each of the 4 `.hiw-step` cards fades up (translateY 12px → 0) with 110 ms stagger. Both use cubic-bezier(0.16,1,0.3,1) ease-out to match the rest of the site's animation language. Observers disconnect after firing to avoid re-animation. Fully respects prefers-reduced-motion. Prior runs had already implemented: metrics strip count-up, reading progress bar, animated r-CBG diagram + replay, spec-card row entrance, app-card stagger, comparison table, milestone timeline structure, hamburger nav, floating CTA, scroll reveal.

## 2026-05-22 — Reading progress bar — No new files in Additional info/ (all 7 already processed). Added a fixed 2px accent-blue reading progress bar at the very top of the viewport (z-index 200, above sticky nav), tracking scroll position via `transform: scaleX()` on a `will-change` element — GPU-accelerated, no layout thrash. Passive scroll listener; `scaleX(0)→scaleX(1)` as user scrolls from top to bottom. Hidden under `prefers-reduced-motion`. Mirrors the editorial progress-indicator pattern used by the FT and NY Times — appropriate for a long technical page with multiple sections.

## 2026-05-21 — No new files; all features already present in dev (metrics strip, count-up, How It Works, diagram animations) — Confirmed all 7 files in Additional info/ already processed. Remote dev branch was ahead with animated metrics strip, count-up counters, "How It Works" 4-step section, animated SVG diagram with replay button, and spec-card row entrance animation — all implemented in prior nightly runs. No net change to index.html this run; log entry added to record the audit.

## 2026-05-20 — r-CBG diagram replay button — No new files in Additional info/ (all 7 already processed). The existing scroll-triggered SVG animation (rays + spectrum bars) had no way to replay once fired. Added a ↺ Replay button below the diagram: clicking removes and re-adds the `.diagram-animated` class via double rAF, restarting the full sequence. CSS scoped to `.diagram-replay`; hidden under `prefers-reduced-motion`. No new library, ~10 lines of code.

## 2026-05-19 — Spec-card row entrance animation + pulse-shaping content from MIT PPTX — No new files (all 7 already processed). From Miniature spectrometer_MIT.pptx, extracted previously unused content: cascaded r-CBG ultrafast pulse shaping (100 fs demonstrated) and space-time wave packet synthesis (25×25×8 mm³) — added as a closing sentence to the X-CBG paragraph in the Technology section. Feature: staggered left-slide entrance animation for all 10 spec-card rows (45–1000 ms range, 55 ms per row); CSS uses translateX(-8px)→none with ease-out cubic, scoped to .js-animations, prefers-reduced-motion safe; JS uses setTimeout cascade on page load.

## 2026-05-18 — "How It Works" 4-step process section — No new files in Additional info/ (all 7 already processed). Added #how-it-works section between the masthead and #technology: a 4-column card grid walking through (1) light input, (2) internal dispersion, (3) CCD capture, (4) Bluetooth readout. Addresses the gap where the technology section is rich prose but has no quick visual entry point for non-technical visitors. Consistent with dark editorial aesthetic: var(--surface) card backgrounds, IBM Plex Mono step labels in accent2, Playfair Display h3 headings, hover lift to var(--raised). Fully responsive (stacks to 1-column on mobile). Uses existing scroll-reveal class — no new JS required.

## 2026-05-17 — Animated r-CBG spectrometer diagram — No new files in Additional info/ (all 7 already processed). Added scroll-triggered sequential animation to the inline SVG diagram in the Technology section: the input beam pulses with a continuous glow (svgBeamPulse, 2.5 s infinite), the 6 dispersed wavelength rays (λ₁–λ₆) and their labels fade in one by one with 120 ms stagger, and the reconstructed Raman spectrum bars grow upward from the baseline (scaleY 0.08→1) in sequence — completing the full sequence at ~1.9 s. Pure CSS keyframes + one IntersectionObserver (threshold 35%), fires once. prefers-reduced-motion safe.

## 2026-05-16 — Animated metrics strip (count-up numbers) + publications reveal fix — No new files in Additional info/ (all 7 already processed). Added a four-column metrics strip between the masthead and Technology section displaying the four key performance claims (10×, 60×, ~5 cm², 95%) as large serif numerals that count up from zero via eased requestAnimationFrame animation when scrolled into view; respects prefers-reduced-motion. Also fixed missing scroll-reveal class on the Publications section.

## 2026-05-15 — Company milestone timeline + University of Utah collaboration mention — No new files; feature run only

### Part 1 — Files processed
All 7 files in `Additional info/` were already in `processed_files.md` from prior runs. No new files found.

### Part 2 — Website improvement: Company milestone timeline
Added `#milestones` section between About and Partners — a vertical timeline showing the company's journey from 2023 (CREOL demonstration) through 2024 (founding, UCF license, I-Corps), 2025 (NSF STTR Phase I, MIT presentation), Feb 2026 (AR-coating validation), and a forward-looking 2026 milestone (prototype completion, pilot deployments). Each item uses a dot on a vertical rule; key milestones use the accent blue; the NSF award uses the accent orange; the final future milestone is visually neutral. Implements with pure CSS (no JS needed — uses existing scroll-reveal class). Respects existing dark editorial aesthetic.

Also updated About text to include the University of Utah (ECE department) collaboration — sourced from the `Miniature spectrometer_MIT.pptx` co-author list (T.M. Hayward, R. Menon) — not yet reflected in the body copy.

## 2026-05-14 — Staggered use-case card entrance + spec-row hover micro-interactions — No new files in Additional info/ (all 7 already processed by prior runs). Added CSS/JS staggered cascade animation to the 6 Use Case cards using IntersectionObserver + nth-child transition-delay (0–375 ms), so cards roll in as a wave rather than all at once; respects prefers-reduced-motion. Added spec-row micro-interaction: on hover each row indents 0.6 rem, brightens the label to text color and lightens the accent value — creating a subtle data-table feel consistent with the editorial dark aesthetic.

## 2026-05-13 — Sticky floating "Request a Demo" CTA — No new files in Additional info/ since last run (all 7 already processed). Added a fixed-position "Request a Demo" button that fades in via IntersectionObserver once the masthead scrolls out of view, and auto-hides when the contact section becomes visible — keeping the conversion nudge persistent without obscuring the destination.

## 2026-05-12 — Competitor comparison table + scroll-reveal animations — The comparison table makes SpectraMini's key advantages (10× smaller, 60× throughput, fewer parts, lower cost) immediately scannable against Portable and Benchtop Raman, while IntersectionObserver-based scroll reveals give every section a polished, purposeful entrance without any external libraries.

## 2026-05-12 — Added r-CBG grating dimensions + AR-coating validation news + restored publications + enriched spec card — Extracted 6 PPTX files (PDF skipped, no pdftotext); key new facts: grating element is 3×6×25 mm, AR-coated samples achieved 2.4 nm FWHM at 900–920 nm in Feb 2026. Added r-CBG element row to spec card, restored PTR glass + X-CBG + Bluetooth paragraphs in technology section, added Feb 2026 and May 2025 MIT news items, restored publications section with Research nav link. Merged with existing comparison table and scroll-reveal implementation.

## 2026-05-12 — Mobile hamburger nav — All 7 files in Additional info/ confirmed processed. Implemented accessible hamburger menu: animated 3-bar icon (CSS transforms, no images) toggles a full-width frosted-glass dropdown on mobile, closes on link click or outside tap, with aria-expanded for screen readers — fixing the critical gap where mobile visitors previously had zero navigation.

## 2026-06-20 — Resolution comparison visualization added to Technology section — No new files in Additional info/ (all 7 already processed). Part 0.5 content cleanup: all previously mandated removals (Milestones section, External Characterization news, MIT news item) confirmed already absent from prior sessions; UCF spinout news item confirmed present; I-Corps item confirmed clean (no fentanyl/beachhead language). No changes required for Part 0.5. Part 2 improvement: added a side-by-side peak-resolution comparison panel (.res-compare) inside the #technology section, between the tech-grid prose and the spectral coverage bar. The panel renders two physics-accurate Gaussian SVG plots: (1) two peaks 20 cm⁻¹ apart shown smeared into one unresolved broad mound at 0.5–2 nm resolution (conventional portable), (2) the same peaks cleanly separated at ~0.1 nm resolution (SpectraMini r-CBG). New CSS tokens --diffraction-blur and --diffraction-peak use optics vocabulary. Entrance animation fires once via IntersectionObserver (threshold 0.15), second panel delayed 180 ms. Fully responsive (stacks to 1-col below 600px), respects prefers-reduced-motion. 4-check: Swap test — physics-accurate Gaussian spectral peaks are unique to an optics company; Squint test — one broad grey mound vs two sharp blue spikes is unmistakable at any blur level; Signature test — r-CBG badge, 20 cm⁻¹ vocabulary, blue accent peaks, P₁/P₂ callouts, optics-paper layout; Token test — --diffraction-blur / --diffraction-peak evoke diffraction-limit vocabulary.
